---
identity:
  role: $identity_role
  industry: $identity_industry
  archetype: $identity_archetype
goals:
  top_problems: $goals_top_problems
  desired_outcomes: $goals_desired_outcomes
preferences:
  project_style: $preferences_project_style
  coding_languages: $preferences_coding_languages
  skill_tolerance: $preferences_skill_tolerance
activity:
  project_count: $activity_project_count
  total_skill_uses: $activity_total_skill_uses
  top_projects: $activity_top_projects
  last_active: $activity_last_active
generated:
  updated_at: $generated_updated_at
  anecdote_count: $generated_anecdote_count
  version: $generated_version
---

<!--
  Tip: wrap any paragraph below in lock-start / lock-end HTML-comment markers
  (the strings "lock:start" and "lock:end" inside <!- - - -> brackets) to
  prevent the heal flow from rewriting it. Locked paragraphs are carried
  through every heal byte-for-byte. Useful for direct quotes, identity claims,
  or anything you want preserved exactly as written. See SKILL.md for the
  exact marker syntax.
-->

$prose
